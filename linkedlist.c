
#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <stdbool.h>
#include "basicdatastruct.h"


int ll_insert(linkedlist_t * ll, const void * value)  {
    
    if (ll == NULL)  { return -1 ; }

    a_node_t * node = malloc(sizeof(a_node_t));

    if (node == NULL)   { return -1;}

    node->next = NULL;
    node->value = value;
   
    ll->
    while ()


}
